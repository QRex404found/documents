# Git에게 텍스트 파일에 대한 자동 줄 끝 문자 변환을 처리하도록 지시합니다.
# 이 설정은 Windows에서는 CRLF를, Unix/Mac에서는 LF를 로컬에 체크아웃하도록 보장합니다.
* text=auto

# (선택 사항) SQL 파일은 저장소 내에서 LF(Linux/Mac/Git 표준)로 저장되도록 명시적으로 지정합니다.
# .sql 파일을 추가했으므로 이 설정을 추가하는 것이 좋습니다.
*.sql text eol=lf